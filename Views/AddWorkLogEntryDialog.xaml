<Window x:Class="Museum.Views.AddWorkLogEntryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавить запись" Height="200" Width="300"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Дата:" Margin="0,5"/>
        <DatePicker x:Name="dpDate" Grid.Row="0" Grid.Column="1" 
                    SelectedDate="{Binding EntryDate}" Margin="5,5,0,5"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Вид работы:" Margin="0,5"/>
        <ComboBox x:Name="cmbWorkType" Grid.Row="1" Grid.Column="1" 
                  ItemsSource="{Binding WorkTypes}" DisplayMemberPath="Title" 
                  SelectedItem="{Binding SelectedWorkType}" Margin="5,5,0,5"/>

        <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10">
            <Button Content="ОК" Click="Ok_Click" Width="80" Margin="0,0,10,0"/>
            <Button Content="Отмена" Click="Cancel_Click" Width="80"/>
        </StackPanel>
    </Grid>
</Window>