<Window x:Class="Museum.Views.SellTicketDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Продажа билета" Height="300" Width="400"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Выставка:" Margin="0,5"/>
        <ComboBox Grid.Row="0" Grid.Column="1" ItemsSource="{Binding Exhibitions}"
                  DisplayMemberPath="Title" SelectedValuePath="Id"
                  SelectedValue="{Binding SelectedExhibitionId}" Margin="5,5,0,5"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Дата посещения:" Margin="0,5"/>
        <DatePicker Grid.Row="1" Grid.Column="1" SelectedDate="{Binding VisitDate}" Margin="5,5,0,5"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Стоимость (руб):" Margin="0,5"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding SalePrice}" Margin="5,5,0,5"/>

        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10">
            <Button Content="Продать" Click="Sell_Click" Width="80" Margin="0,0,10,0"/>
            <Button Content="Отмена" Click="Cancel_Click" Width="80"/>
        </StackPanel>
    </Grid>
</Window>